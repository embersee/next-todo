import Image from 'next/image'
import collapse from '../../public/assets/collapse.svg'

const Collapse = (props: any) => {
  return <Image width={25} height={25} src={collapse} alt='' className='' />
}

export default Collapse
